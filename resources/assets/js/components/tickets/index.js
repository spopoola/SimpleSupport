import Vue from 'vue'

export const TicketRow = Vue.component('ticket-row', require('./TicketRow'))

export const PrioritySelect = Vue.component('priority-select', require('./PrioritySelect'))

export const TicketModal = Vue.component('ticket-modal', require('./TicketModal'))

export const TicketsList = Vue.component('tickets-list', require('./TicketsList'))
